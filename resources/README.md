# Dispytch resources

## Nginx configuration (Debian)

* Required Debian packages
  * nginx
  * fcgiwrap

```
apt-get install nginx fcgiwrap
```

Samples configuration files are available whithin the `resources/nginx`
directory. Modify and copy those files to your nginx configuration directory
for a quick hands-on.

## OpenBSD inetd configuration

`to be documented`
